# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       ApplicationController.index
# newsletter pages
GET     /newsletter                             Newsletter.index
GET     /newsletter/index                       Newsletter.index
GET     /newsletter/upload                      Newsletter.upload

# article pages
GET     /article                             ArticleController.index
GET     /article/index                       ArticleController.index
GET     /article/show                        ArticleController.show
GET     /article/new                         ArticleController.newArticle
GET     /article/new                         ArticleController.newarticle
GET     /article/edit                        ArticleController.edit
GET     /article/delete                      ArticleController.delete
GET     /unpublished/index                   ApplicationController.unpublishedShow

# browse pages
GET     /browse			                     BrowseController.index
GET     /browse/index                        BrowseController.index

# review pages
GET     /review	                     ReviewController.index
GET     /review/index                ReviewController.index

# Import Secure routes
*      /                module:secure

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}

